#include "main.h"
#include <stdio.h>
/**
 * _abs - Computesabsolute value
 * of an integer
 * 1: inputnumber at integer
 * Return: absolutevalue
 */
int _abs(int i)
{`

	if (i >= 0)
{
	return (i);
}
else
{
	return (i * -1);
}
}
